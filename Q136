Create an enum for user roles (ADMIN, USER, GUEST) and display messages based on role

#include <stdio.h>

typedef enum {
    ADMIN,
    USER,
    GUEST
} UserRole;

void displayRoleMessage(UserRole role) {
    switch (role) {
        case ADMIN:
            printf("Welcome, Admin! You have full access.\n");
            break;
        case USER:
            printf("Hello, User! You have limited access.\n");
            break;
        case GUEST:
            printf("Hi, Guest! You can browse as a visitor.\n");
            break;
        default:
            printf("Unknown role.\n");
    }
}

int main() {
    UserRole role;

    role = USER;

    displayRoleMessage(role);

    return 0;
}
